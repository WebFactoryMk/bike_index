%h1
  #{t('info.terms.p3a')}
%p
  #{t('customer-mailer.good-thinking')}

%p{ style: 'margin-bottom: 2em;' }
  #{t('customer-mailer.please-confirm')}

= link_to "#{t('customer-mailer.verify')}", confirm_users_url(id: @user.id, code: @user.confirmation_token), class: 'binx-button'

%p.less-strong.center-text
  #{t('customer-mailer.click-the')} <em#{t('customer-mailer.verify')}</em> #{t('customer-mailer.button-to-finish')}
